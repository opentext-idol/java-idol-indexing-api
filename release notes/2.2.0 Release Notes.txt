# $Id$
# Last modified by $Author$ on $Date$

This is an INTERNAL ONLY release and is being made only to ensure that IDOL ATTACH for Hadoop has minimal 3rd party
dependencies. The intent, is that there will be a new Indexing API that will be Java 7 based to replace Non-ACI.

New Features
 1. HttpClient updated to the latest version. Command Lang updated to latest version, which requires Java 6, so the
    project has been moved to Java 6.

Resolved Issues
 1.
